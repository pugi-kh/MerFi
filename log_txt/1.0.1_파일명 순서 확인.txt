01. 몇번째부터 몇번째까지인지 모를때
    파일 하나의 이름을 붙여넣어서 각 문자마다 몇번째인지 숫자 출력

02. 폴더명 출력 코드 단순화

===========기존===========
for i in range(len(list)):
    print(list[i], end = "")
    if i < len(list) - 1 :
        print(" / ", end = "")

===========변경===========
print(" / ".join(folder_list))


03. 폴더 여러개 추가제거시 오류 해결

folder_temp 라고 임시로 만들어 놓은 세트를 반복해서 사용하도록 했는데
아무 값을 입력하지 않을 때
이전에 사용했던 값이 그대로 사용되어
코드를 실행하고 빈 세트로 변경하도록 추가
